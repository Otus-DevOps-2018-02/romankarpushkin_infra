---
# ansible
- name: Install app packages
  hosts: all
  become: true
  tasks:
  - name: Install ruby and stuff
    apt: "name={{ item }} state=present"
    with_items:
      - ruby-full
      - ruby-bundler
      - build-essential
